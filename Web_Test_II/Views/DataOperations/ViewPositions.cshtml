@using Web_Test_II_DAL.Entityes;
@using Web_Test_II.Models.ViewModels.DataOperationsViewModels;
@model PositionsViewModel

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, Web_Test_II

@{
    int num = 0;
    ViewBag.Title = "Просмотр и добавление позиций";
     
    
}

<div class="text-center">

        <h2 class="display-4">Должности</h2>  

        <table class="table">
            <tr>
                <form asp-controller="DataOperations" asp-action="AddPosition" method="post">
                 <th>
                   <input type="text" class="form-control" asp-for="Position.Name"/>
                    <span asp-validation-for="Position.Name"/> 
                </th>
                <th>
                  <input type="submit" class="btn btn-success" value="Добавить должность"/>
                </th>
                </form>
            </tr>
           <</table>

              
        <table class="table">
            <tr>
                 <th>
                    <a>Номер</a>
                </th>
                <th>
                    <a>Должность</a>
                </th>
                <th>
                    <a>Редактирование</a>
                </th>
                <th>
                    <a>Удаление</a>
                </th>
            </tr>
            
            @foreach(var position in Model.Positions)
            {
                num++;
                <tr>
                    <td>
                        @num
                    </td>  
                    <form asp-controller="DataOperations" asp-action="EditPosition" method="post"  asp-route-id="@position.Id">
                    <td>
                       <input type="text" class="form-control" name="parameterName" value="@position.Name"/>
                    </td>  
                    <td>
                        <input type="submit" value="Редактировать" class="form-control" />
                    </td>
                    </form>
                    <td>
                        <form asp-controller="DataOperations" asp-action="DeletePosition" method="post"  asp-route-id="@position.Id">
                            <input type="submit" value="Удалить" class="form-control" />
                            </form>
                    </td>
                </tr>           
        }
        </table>
        

    
</div>